DESCRIPTION

apertium-subtitles translates subtitles with Apertium. Only SRT files are
currently supported.

BUILD

With the default build option, you'll probably need some libraries or
project properties (which are correctly set when the project is opened with
NetBeans): 

$ ant

You can also use this simple build file:

$ ant -f simple-build.xml


RUN

$ java -jar dist/apertium-subtitles.jar
