#!/bin/bash

# Runs a corpus a test - compares the output of the last revision with
# the current one.

# See http://wiki.apertium.org/wiki/Corpus_test

cat corpa/corpus.kaz.txt | apertium -d . kaz-tat > corpa/kaz-tat-nova.txt &&

diff -U0 corpa/kaz-tat-origina.txt corpa/kaz-tat-nova.txt | dwdiff -c --diff-input | more
